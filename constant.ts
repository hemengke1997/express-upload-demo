const PORT = 8080 // 服务端口
const BASE_URL = `http://localhost:${PORT}/` // 服务url
const MAX_SIZE = 100 // 文件最大体积（MB）
const FILE_TARGET = 'static' // 文件存储目录

export { BASE_URL, MAX_SIZE, PORT, FILE_TARGET }
